export const metadata = {
    title: "Register | Orange IDP",
    description: "Register your identity using OrangeIDP",
}

export default async function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
      <>
        {children}
      </>
  );
}